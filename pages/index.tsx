import type { NextPage } from 'next';

const Home: NextPage = () => {
   return (
      <section>
         <div>
            <h1>Viandas Cook</h1>
         </div>
      </section>
   );
};

export default Home;
